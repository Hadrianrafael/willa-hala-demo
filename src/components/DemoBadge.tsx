/** Identificação discreta de que este é um protótipo não oficial. */
export function DemoBadge() {
  return (
    <div
      role="note"
      className="pointer-events-none fixed bottom-3 left-3 z-40 bg-navy-900/90 px-3 py-1.5 text-[0.58rem] font-medium uppercase tracking-[0.2em] text-white backdrop-blur-md sm:bottom-4 sm:left-4"
    >
      Demonstração não oficial · HR Tech
    </div>
  );
}
